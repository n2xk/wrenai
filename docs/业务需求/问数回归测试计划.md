# 问数回归测试计划 v2（TiDB workspace / MCP Playwright）

## 1. 测试背景

本计划用于验证 `docs/业务需求` 目录下沉淀的业务需求，是否已经在系统内形成可用、可命中、可准确回答的问数能力，并能够在 **TiDB workspace** 中完成问答、追问、图表生成和缺失外部数据处理。

随着业务知识配置中心功能完成，当前知识库回归范围已经从：

```text
分析规则 + SQL 模板
```

扩展为：

```text
分析规则 + SQL 模板 + 业务词典 + 外部数据依赖 + 结构化知识字段
```

本轮测试要求：

- **全部通过 MCP Playwright 执行业务链路操作、导入、问数和结果采集**
- **全部在 Chrome 中操作系统 UI / 同源 API**
- **全部在 TiDB workspace 中完成验证**
- **完整回归必须先清理历史测试数据并重建本轮专用 TiDB workspace**
- **除用户明确声明“跳过 setup / 复用现有环境”外，不允许复用旧 workspace、旧知识库、旧问数线程或旧知识资产**
- 统一以 **TiDB SQL 能力链路** 作为唯一主链路，不以 ES 作为当前执行链路
- 数据库查询只作为可选旁证，不替代 MCP Playwright 验收结论

---

## 2. 测试目标

本轮目标分为四个层面。

### 2.1 覆盖性验证

验证 `docs/业务需求` 中的业务需求，是否已经被系统中的以下能力覆盖：

- 分析规则（instruction）
- SQL 模板（sql pair / sql template）
- 业务词典（business dictionary / business term）
- 外部数据依赖（external dependency）
- SQL 模板结构化字段（`business_signature`）
- 分析规则结构化字段（`related_business_terms` / `related_external_dependencies` / `runtime_usage`）
- suggested questions
- modeling AI assistant 生成的语义提示
- modeling AI assistant 生成的关联关系

### 2.2 准确性验证

验证系统对这些业务需求的以下输出是否准确：

- 问数结果
- 追问结果
- 图表生成结果
- 业务词典同义词命中结果
- SQL 模板匹配与参数抽取结果
- 缺失外部数据源时的追问与兜底行为

### 2.3 产品化验证

验证业务域特化语义是否由知识库配置驱动，而不是依赖代码硬编码：

- “首存 / 首充 / 首次存款”应由 `first_deposit` 业务词典定义驱动
- “续存 / 二存 / 六存 / 复存”应由 `retention_deposit` 业务词典定义驱动
- “TOP3 / 非TOP3”应由 `topn_segment` 业务词典定义驱动
- “ROI / 首存成本”应通过外部依赖 `ad_spend` 触发缺失数据追问
- “PV / UV / 下载点击UV”应通过外部依赖触发缺失数据追问

### 2.4 UI 体验观察

在 MCP Playwright 执行 UI 测试过程中，同步记录真实使用体验与产品可用性问题。重点观察：

- workspace 创建、知识库创建、数据源连接、建模、导入、问数、追问、图表生成等关键流程是否顺畅
- 表单字段、按钮文案、错误提示、加载状态、空状态是否清晰
- 批量导入、长任务等待、失败重试、成功反馈是否容易理解
- 业务词典、外部数据依赖、SQL 模板、分析规则四类资产的管理入口是否一致、易发现、易操作
- 问数结果、diagnostics、外部依赖阻断提示是否足够可解释

体验问题与优化建议需要单独沉淀到：

- `docs/业务需求/UI体验优化记录-YYYY-MM-DD.md`

---

## 3. 测试范围

### 3.1 分析规则

以 `docs/业务需求/knowledge-base/analysis-rules/` 为权威来源，验证共 **14** 条分析规则是否在系统中生效：

- R01 ~ R14
- 重点关注：首存定义、新客首存、TOPN、投充比、杀率、ROI、续存、首存金额分桶、缺失数据源处理、ES 数据使用限制

### 3.2 SQL 模板

以 `docs/业务需求/knowledge-base/sql-templates/` 为权威来源。

其中：

- **11 个 `draft_sql` 模板**作为本轮可执行问数主范围：
  - T01
  - T02
  - T03
  - T04
  - T06
  - T08
  - T09
  - T10
  - T11
  - T12
  - T13
- **3 个阻塞模板**作为异常/兜底行为验证范围：
  - T05
  - T14
  - T15

> 说明：T05/T14/T15 因缺少外部数据源，不作为可执行 SQL pair 主链路导入对象；本轮仍需要验证系统在命中相关问题时，是否按照 `R13_缺失数据源处理` 和外部数据依赖配置先向用户索取外部指标，而不是直接编造答案。

### 3.3 业务词典

以 `docs/业务需求/knowledge-base/business-dictionary/` 为权威来源，验证共 **10** 条业务词典是否导入并参与问数匹配：

- `bet_deposit_ratio`：投充比
- `develop_user`：开发人数
- `first_deposit`：首存
- `first_deposit_cost`：首存成本
- `kill_rate`：杀率
- `new_customer_first_deposit`：新客首存
- `retention_deposit`：续存
- `roi`：ROI
- `topn_segment`：TOPN 分层
- `traffic_metrics`：流量指标

重点验证：

- `aliases` 能被问数召回
- `definition` / `canonical_expression` 能约束解释与模板匹配
- `related_rules` / `related_templates` 能辅助 instruction 和 SQL 模板选择
- `conflict_terms` 不造成错误概念替换

### 3.4 外部数据依赖

以 `docs/业务需求/knowledge-base/external-dependencies/` 为权威来源，验证共 **4** 条外部数据依赖是否导入并参与缺失数据处理：

- `ad_spend`：投放金额
- `access_pv`：访问PV
- `access_uv`：访问UV
- `download_click_uv`：下载点击UV

重点验证：

- `source_status = missing` 生效
- `missing_behavior = ask_user` 生效
- `required_grain` 能转化为正确索取粒度
- `required_by_terms` / `required_by_templates` 能驱动 ROI、首存成本、流量指标问题阻断
- `ask_user_prompt` 的语义能体现在系统回答中

### 3.5 结构化知识字段

本轮需要验证以下结构化字段是否进入导入与运行链路：

- SQL 模板：`business_signature`
  - `concepts`
  - `features`
  - `metrics`
  - `dimensions`
  - `parameter_slots`
  - `external_dependencies`
  - `positive_cues`
  - `negative_cues`
  - `expected_grain`
- 分析规则：
  - `related_business_terms`
  - `related_external_dependencies`
  - `runtime_usage`
- 运行态模板元数据：
  - `source_type = business_import`
  - `template_level = L2`
  - `template_mode = anchored_template`

---

## 4. 权威输入文档

### 4.1 导入来源

- `docs/业务需求/knowledge-base/analysis-rules/`
- `docs/业务需求/knowledge-base/sql-templates/`
- `docs/业务需求/knowledge-base/business-dictionary/`
- `docs/业务需求/knowledge-base/external-dependencies/`
- `docs/业务需求/knowledge-base/ui-import-checklist.md`
- `docs/业务需求/knowledge-base/import-format.md`
- `docs/业务需求/knowledge-base/import-manifest.sample.yaml`

### 4.2 测试与验收依据

- `docs/业务需求/test-runbook.md`
- `docs/业务需求/expected-results.md`
- `docs/业务需求/extended-seed-results.md`
- `docs/业务需求/csv/00_测试参数与总校验点.csv`
- `docs/业务需求/csv/01_T01_渠道日基础汇总.csv` ~ `11_T13_首存金额分桶.csv`
- `docs/业务需求/csv/99_回归执行清单.csv`

### 4.3 数据准备依据

- `docs/业务需求/local_tidb_schema.sql`
- `docs/业务需求/seed.sql`
- `docs/业务需求/generate_local_tidb_schema.py`

### 4.4 环境事实与回归记录

- `docs/业务需求/当前TiDB workspace知识资产快照-2026-04-26.md`
- `docs/业务需求/问数回归测试结果-2026-04-26.md`

---

## 5. 固定测试参数

本轮测试统一使用以下参数：

| 参数 | 值 |
| --- | --- |
| tenant_plat_id | 990001 |
| 主测试渠道 channel_id | 990011 |
| 对照渠道 channel_id | 990012 |
| start_date | 2026-04-01 |
| end_date | 2026-04-07 |
| cohort_start_date | 2026-04-01 |
| cohort_end_date | 2026-04-03 |
| top_n | 3 |
| n_days | 7 |
| period_days | 7 |

总校验点：

- 主渠道总充值金额 = `3248`
- 主渠道总提现金额 = `160`
- 主渠道总有效投注 = `7300`
- 主渠道总输赢 = `580`
- TOP3 玩家 = `990101 / 990102 / 990103`

---

## 6. 测试前置条件

在 UI 测试开始前，需确认以下前置条件成立。

### 6.1 完整回归环境清理硬性要求

完整回归默认必须从干净环境开始。除非用户明确要求“跳过 setup / 复用现有环境”，否则必须清理所有历史测试数据，并重新创建本轮专用 TiDB workspace。若由于审计、留痕或环境权限原因无法物理删除历史数据，必须使用全新隔离环境替代，并在最终报告中列出“未物理删除但已隔离”的对象。

必须清理的历史对象包括：

> 若无法物理删除，必须用新建对象隔离并记录原因。

| 类型 | 清理要求 |
| --- | --- |
| workspace | 不复用历史测试 workspace；完整回归必须新建 TiDB workspace |
| knowledge base / snapshot | 不复用旧知识库、旧 snapshot、旧 deploy hash |
| connector / data source | 不复用旧 connector；完整回归需重新连接 TiDB 数据源 |
| project / project_meta | 不复用旧项目、旧建模元数据 |
| model / relation / metric / view | 不复用旧 modeling 结果；需由本轮建模或导入产生 |
| instruction / sql_pair | 不复用旧分析规则、旧 SQL 模板记录；需由本轮导入产生 |
| business terms | 不复用旧业务词典记录；需由本轮导入产生 |
| external dependencies | 不复用旧外部数据依赖记录；需由本轮导入产生 |
| thread / thread_response / asking_task | 不复用旧问数线程、旧回答、旧任务结果 |
| suggested questions | 不复用旧建议问题；需由本轮数据源配置后重新生成 |

清理后必须记录验证证据：

- 旧 workspace / knowledge base 不再被本轮使用
- 本轮 workspace / knowledge base / connector / deploy hash 已明确记录
- 本轮知识库在导入前四类知识资产计数为 0，或能证明它是新建空知识库
- 本轮问数线程从本轮测试开始生成，不混入旧 thread / response

### 6.2 TiDB 测试库准备硬性要求

完整回归必须清空并重建 TiDB 测试库，或使用全新的隔离 TiDB 测试库。不能在带有未知历史数据的 TiDB 库上标记“全量干净回归通过”。

必须执行：

1. 执行 `python3 docs/业务需求/generate_local_tidb_schema.py`
2. 清空 / 重建目标 TiDB 测试 schema
3. 导入 `docs/业务需求/local_tidb_schema.sql`
4. 导入 `docs/业务需求/seed.sql`
5. 校验固定测试参数与总校验点：`tenant_plat_id=990001`、主渠道 `990011`、对照渠道 `990012`、主渠道总充值 `3248`、总有效投注 `7300`

若用户明确要求复用现有 TiDB 测试库，只能执行最小回归或非全量回归，并必须在最终报告中标记为“非全量干净回归”。

### 6.3 复用现有环境的例外条件

只有用户明确要求“跳过 setup / 复用现有环境 / 继续当前 workspace”时，才允许跳过完整清理。此时最终报告必须明确标记为：

```text
非全量干净回归：复用了现有 TiDB workspace / knowledge base
```

并记录：

- 复用的 `workspace_id`
- 复用的 `knowledge_base_id`
- 复用的 `kb_snapshot_id`
- 复用的 `deploy_hash`
- 复用前已有知识资产数量
- 复用前已有问数线程是否会影响本轮统计

### 6.4 基础服务要求

1. 本轮使用的数据源必须能支撑 TiDB workspace 中的语义建模、知识库导入与问答验证。
2. UI 与 AI service 均处于可用状态。
3. MCP Playwright 使用 Chrome 作为浏览器载体。

---

## 7. 执行约束

本轮执行必须满足以下约束：

- 仅使用 **MCP Playwright** 完成 UI 操作、同源 API 调用、问数、追问、图表生成和结果采集。
- 使用 **Chrome** 作为浏览器执行载体。
- 所有业务需求验证都必须在 **TiDB workspace** 内完成；不允许在 MySQL、PostgreSQL、SQLite、legacy ES workspace 或其他非 TiDB workspace 中替代执行。
- 当前主链路仅验证 TiDB 能力，不将 legacy ES 作为独立 sql_pair 执行来源。
- 完整回归必须先完成第 6.1 节的数据清理 / 隔离要求；未完成清理不得标记为“全量干净回归通过”。
- 若用户要求复用环境，最终报告必须按第 6.3 节标记为“非全量干净回归”。
- 对于缺失外部数据源的问题，不允许系统直接编造投放金额、PV、UV、下载点击 UV 等值。
- 对于阻塞模板，不以“执行 SQL 成功”为通过标准，而以“识别缺失依赖并正确索取外部数据”为通过标准。
- 对于业务词典，不以“页面存在”为唯一通过标准，必须通过问数 alias 命中验证其运行时生效。

---

## 8. 测试执行流程

### Step 1：重新初始化并清理系统环境

目标：确保完整回归使用干净环境，避免旧 workspace、旧知识库、旧建模结果、旧问数线程、旧知识资产干扰。

完整回归必须执行：

- 清理历史测试 workspace；若无法物理删除，必须新建隔离 TiDB workspace 并记录原因
- 清理历史 knowledge base / snapshot / deploy hash；若无法物理删除，必须新建隔离知识库并记录原因
- 清理历史 connector / project / modeling 结果；若无法物理删除，必须新建隔离对象并记录原因
- 清理历史 instruction / sql_pair / business term / external dependency；若无法物理删除，必须确认本轮知识库导入前为空
- 清理历史 thread / thread_response / asking_task；若无法物理删除，必须确认本轮统计不包含旧线程
- 清空并重建 TiDB 测试库 schema 与 seed，或使用全新隔离 TiDB 测试库

检查点：

- 本轮新建 TiDB workspace，且 workspace 类型为 TiDB
- 本轮新建知识库，导入前四类知识资产计数为 0，或有等价证据证明知识库为空
- 本轮新建或重新配置 TiDB connector
- 本轮问数线程从本轮测试开始生成
- 若用户明确要求复用环境，必须记录为“非全量干净回归”，并记录复用对象 ID 与复用前资产数量

### Step 2：通过 MCP Playwright 拉起 Chrome 并创建 TiDB workspace

目标：在系统 UI 内创建一套本轮专用的 TiDB workspace。

操作路径：

- 系统设置
- 工作空间管理
- 新建 **TiDB workspace**

检查点：

- workspace 创建成功
- workspace 类型明确为 TiDB
- 不创建、不切换、不复用任何非 TiDB workspace 做本轮验证
- 后续建模、知识库、问答链路都在该 TiDB workspace 下进行

### Step 3：新增知识库

目标：为本轮四类知识资产导入提供承载容器。

检查点：

- 知识库创建成功
- 知识库归属到本轮 TiDB workspace
- 知识库页面包含或可进入以下资产管理入口：
  - 分析规则
  - SQL 模板
  - 业务词典
  - 外部数据依赖

### Step 4：添加资产并连接 TiDB 数据源

目标：把当前回归数据接入 TiDB workspace。

检查点：

- TiDB 数据源可连接
- 连接参数正确
- 相关表可被系统识别

### Step 5：配置数据源并生成建议问题

目标：完成数据源可用性配置，并验证 suggested questions 可生成。

检查点：

- 数据源配置保存成功
- suggested questions 成功生成
- suggested questions 与业务主题具备相关性

### Step 6：用 modeling AI assistant 生成语义提示

目标：为后续问数补齐语义层提示信息。

检查点：

- 语义提示生成成功
- 生成结果与 TiDB 资产语义一致
- 能支撑业务术语问数

### Step 7：用 modeling AI assistant 生成关联关系

目标：补齐表之间或模型之间的关联关系，降低问数歧义。

检查点：

- 关联关系生成成功
- 关系方向、主外键或业务关联合理
- 能支持跨表统计问数

### Step 8：导入分析规则

导入来源：

- `docs/业务需求/knowledge-base/analysis-rules/`

目标：让系统先具备正确的业务口径和异常处理边界。

重点验证：

- `R13_缺失数据源处理` 已生效
- `R14_ES 数据使用限制` 已生效
- 首存、新客首存、TOPN、VIP、ROI 收入口径等规则可约束回答
- `related_business_terms` / `related_external_dependencies` / `runtime_usage` 被保存或进入运行时索引

### Step 9：导入业务词典

导入来源：

- `docs/业务需求/knowledge-base/business-dictionary/`

目标：让业务概念、同义词、规范表达式、关联规则和模板由知识库配置驱动。

检查点：

- 10 条业务词典全部导入
- `termId` 唯一且与文档 `id` 一致
- `aliases`、`definition`、`canonicalExpression`、`relatedRules`、`relatedTemplates`、`features`、`conflictTerms` 字段正确保存
- 业务词典进入 AI instruction / retrieval 索引，能参与问数匹配

### Step 10：导入外部数据依赖

导入来源：

- `docs/业务需求/knowledge-base/external-dependencies/`

目标：让缺失外部指标由结构化配置驱动，而不是代码硬编码或模型猜测。

检查点：

- 4 条外部数据依赖全部导入
- `dependencyId` 唯一且与文档 `id` 一致
- `sourceStatus = missing`
- `missingBehavior = ask_user`
- `requiredGrain`、`requiredByTerms`、`requiredByTemplates`、`relatedRules`、`askUserPrompt`、`validation` 字段正确保存
- 外部依赖进入 AI instruction / retrieval 索引，能参与 ROI / 首存成本 / 流量指标问题阻断

### Step 11：导入 SQL 模板

导入来源：

- `docs/业务需求/knowledge-base/sql-templates/`

导入策略：

- 导入 11 个 `draft_sql` 模板作为主测试范围
- T05/T14/T15 保持 `blocked_missing_source`，不作为可执行 SQL pair 主链路
- 所有 SQL 模板的 `business_signature` 均需被保存或参与运行时匹配

目标：让系统具备可命中的 TiDB SQL 模板问数能力。

检查点：

- 11 个主模板可导入为业务 SQL 模板
- 问题变体可展开为 SQL pair / sql template 记录
- 可执行模板进入 `source_type = business_import`
- 可执行模板进入 `template_level = L2`
- 可执行模板进入 `template_mode = anchored_template`
- 参数槽结构化可用于参数抽取与校验

### Step 12：导入后资产与索引校验

目标：确认四类知识资产不仅页面可见，而且真实进入运行时可用状态。

检查点：

- UI/API 可列出 14 条分析规则
- UI/API 可列出 10 条业务词典
- UI/API 可列出 4 条外部数据依赖
- UI/API 可列出 11 个主测试 SQL 模板或其问题变体
- 业务词典 instruction 索引存在：`knowledge_asset_type = business_term`
- 外部依赖 instruction 索引存在：`knowledge_asset_type = external_dependency`
- SQL 模板 diagnostics 可看到 `business_import / L2 / anchored_template`

### Step 13：执行问数 / 追问 / 图表 / 结构化知识验证

使用 `docs/业务需求` 中的测试问题与标准答案进行验收，验证以下五类能力：

#### 13.1 规则类问题验证

先验证系统是否被正确约束，例如：

- 首存定义
- 新客首存
- TOPN 口径
- VIP 分层口径
- 缺失数据源处理
- ES 数据使用限制

#### 13.2 业务词典类问题验证

验证业务词典 alias 是否真实参与问数匹配，例如：

- “首充” -> `first_deposit`
- “二存 / 六存 / 复存” -> `retention_deposit`
- “TOP3 / 非TOP3” -> `topn_segment`
- “投充比” -> `bet_deposit_ratio`
- “杀率” -> `kill_rate`
- “ROI / 首存成本” -> `roi` / `first_deposit_cost`

推荐专项用例：

> 统计租户平台990001下渠道990011在2026-04-01到2026-04-03首充 cohort 的二存到六存人数、比率和均额

预期：

- 命中 `first_deposit` 与 `retention_deposit`
- 命中 T08 或等价模板
- 返回结果与 T08 标准答案一致
- diagnostics 中可看到业务模板锚定信息

#### 13.3 模板类问题验证

验证系统是否能命中对应 SQL 模板并返回正确结果，例如：

- 综合日报
- 渠道与折扣映射
- cohort 收入
- TOPN 分层
- 日龄趋势
- 游戏类型分布
- 首存金额分桶

#### 13.4 追问验证

验证系统是否能继承上文语境，例如：

- 对 T09 追问“那只看非TOP3呢？”
- 对 T04 追问“那只看 2026-04-02 的首存 cohort 呢？”

预期：

- 日期范围、渠道、cohort、TOPN 条件不丢失
- 结果与标准答案一致

#### 13.5 图表验证

对可图表化问题执行图表生成，例如：

- T01：渠道日报趋势
- T04：cohort 累计收入趋势
- T09：ALL/TOP3/非TOP3 分组对比
- T10：日龄趋势
- T11：游戏类型分布
- T12：分层游戏类型对比
- T13：首存金额分桶

预期：

- 图表状态为 `FINISHED`
- 图表类型与问题意图匹配
- 图表数值与 preview 数据一致

#### 13.6 外部数据依赖阻断验证

验证系统在命中以下需求时是否先索取用户输入，而不是直接输出伪造结果：

- ROI
- 投放金额并表
- 流量指标并表
- 首存成本
- UV 下载率 / UV 注册率

推荐专项用例：

> 统计租户平台990001下渠道990011在2026-04-01到2026-04-03首存cohort从D1到D7的ROI

预期：

- 识别 `roi`
- 识别外部依赖 `ad_spend / 投放金额`
- 要求用户补充投放金额
- 不编造 ROI

推荐专项用例：

> 统计租户平台990001下渠道990011在2026-04-01到2026-04-06的渠道日报，并补充PV、UV、下载点击UV、UV下载率和UV注册率

预期：

- 识别 `access_pv / 访问PV`
- 识别 `access_uv / 访问UV`
- 识别 `download_click_uv / 下载点击UV`
- 要求用户按日期、渠道补充外部指标
- 不编造 PV / UV / 下载点击 UV / UV 下载率 / UV 注册率


### Step 14：记录 UI 体验问题与优化建议

目标：在完成 UI 回归测试的同时，沉淀真实操作过程中的体验问题，作为后续产品优化输入。

记录文档：

- `docs/业务需求/UI体验优化记录-YYYY-MM-DD.md`

记录范围：

- workspace / 知识库 / 数据源配置流程
- modeling AI assistant 语义提示与关联关系生成流程
- 分析规则、SQL 模板、业务词典、外部数据依赖的新增、编辑、导入、列表、搜索、删除流程
- 问数、追问、图表生成、外部数据依赖阻断提示流程
- 长任务等待、失败重试、错误提示、成功反馈、空状态、加载态

每条体验记录建议包含：

| 字段 | 说明 |
| --- | --- |
| 场景 | 出现问题的功能路径或操作步骤 |
| 现象 | 用户实际看到的问题或困惑点 |
| 影响 | 对测试、业务用户或配置效率的影响 |
| 严重度 | P0 / P1 / P2 / P3 |
| 建议 | 可执行的优化建议 |
| 证据 | 截图、URL、Playwright 记录、接口错误或控制台日志 |

通过标准：

- 即使 UI 回归全部通过，也需要输出一份体验记录文档
- 若无明显体验问题，也要记录“未发现阻塞级体验问题”和观察范围
- 体验问题不默认阻断问数回归通过，但 P0/P1 问题需要在最终测试报告中单独提示

---


---

## 9. 重点验收维度

### 9.1 覆盖性

通过标准：

- 分析规则可被系统命中并影响回答边界
- 11 个主模板问题都能在系统中形成有效问数链路
- 10 条业务词典都能导入，并至少通过关键 alias 用例证明运行时生效
- 4 条外部数据依赖都能导入，并至少通过 ROI / 流量指标用例证明运行时生效
- 结构化字段能进入导入记录、索引或 diagnostics

### 9.2 数值准确性

通过标准：

- 返回值与 `expected-results.md`、`csv/` 中的预期一致
- 关键总校验点一致
- 对照渠道 `990012` 不串入主渠道结果
- 失败充值、分母为 0 等边界场景处理正确

### 9.3 追问准确性

通过标准：

- 系统能继承上文筛选条件、日期范围、渠道范围、cohort 范围
- 追问不会丢失上下文
- 追问后的结果仍与预期答案一致

### 9.4 图表准确性

通过标准：

- 图表类型与问题意图匹配
- 图表维度、指标、排序与数据表一致
- 图表数值与问数结果一致
- 图表中的时间序列、分组、分层结果不发生错位

### 9.5 缺失数据源处理

通过标准：

- 系统明确指出缺失字段
- 系统索取的数据粒度正确
- 用户未补充外部数据前，系统不伪造 ROI / PV / UV / 下载点击 UV 等结果
- 用户补充后，系统可将外部值与内部 SQL 结果拼接输出（若当前产品已支持外部值回填）

### 9.6 diagnostics 可观测性

通过标准：

- 主模板问数可记录 `templateDecision`
- 可观测到 `sourceType = business_import`
- 可观测到 `templateLevel = L2`
- 可观测到 `templateMode = anchored_template`
- 外部依赖阻断回答可观测到缺失依赖集合或等价原因

---

## 10. 推荐验收顺序

建议按下面顺序执行：

1. 创建 TiDB workspace 与知识库
2. 连接 TiDB 数据源
3. 生成 suggested questions
4. 生成语义提示
5. 生成关联关系
6. 导入分析规则
7. 导入业务词典
8. 导入外部数据依赖
9. 导入 SQL 模板
10. 做资产列表与索引校验
11. 按 `docs/业务需求/csv/99_回归执行清单.csv` 做主模板回归：
    1. T02
    2. T01
    3. T03
    4. T08
    5. T10
    6. T04
    7. T06
    8. T09
    9. T11
    10. T12
    11. T13
    12. COMMON 异常点
12. 做业务词典 alias 专项验证
13. 做外部数据依赖阻断专项验证
14. 做追问验证
15. 做图表验证

原因：先完成 workspace 与知识资产闭环，再验证模板数值，最后验证 alias、外部依赖、追问和图表等运行时行为。

---

## 11. 本轮不以“可执行 SQL 模板”验收的内容

以下模板当前不作为可执行 SQL 主链路验收范围：

- T05：缺投放金额
- T14：缺投放金额
- T15：缺 PV / UV / 下载点击 UV

但这些内容仍需做“问答行为正确性”验证，重点是：

- 是否先索取外部指标
- 是否说明哪些结果暂时无法计算
- 是否可先返回不依赖外部数据的内部指标
- 是否命中对应外部数据依赖资产
- 是否不编造外部指标

---

## 12. 产出物

本轮测试完成后，应沉淀以下结果：

1. Playwright 执行记录
2. 关键页面截图
3. 环境清理 / 新建 TiDB workspace / 新建知识库的证据
4. 四类知识资产导入清单
5. 业务词典导入与 alias 命中记录
6. 外部数据依赖导入与阻断记录
7. SQL 模板 `templateDecision` / diagnostics 记录
8. 每个测试问题的系统回答记录
9. 追问记录
10. 图表截图或导出结果
11. 与 `expected-results.md` / `csv/` 的对账结论
12. UI 体验优化记录：`docs/业务需求/UI体验优化记录-YYYY-MM-DD.md`
13. 最终通过 / 失败清单

---

## 13. 通过标准

当以下条件全部满足时，可认为本轮测试通过：

1. 完整回归已完成历史测试数据清理 / 隔离，并有证据证明本轮使用新建 TiDB workspace 与新建知识库。
2. 全部验证均在 TiDB workspace 内完成，没有使用非 TiDB workspace 或 legacy ES 作为替代执行链路。
3. TiDB workspace 从初始化到建模、知识导入、问答链路全部跑通。
4. 14 条分析规则在关键问题上表现符合预期。
5. 10 条业务词典已导入，且关键 alias 用例证明运行时生效。
6. 4 条外部数据依赖已导入，且 ROI / 流量指标阻断用例证明运行时生效。
7. 11 个主测试模板均可被系统覆盖并返回正确结果。
8. 主测试模板 diagnostics 显示业务模板锚定链路正常。
9. 问数、追问、图表生成三类能力均通过验证。
10. 对缺失外部数据源的问题，系统行为符合 `R13` 和外部数据依赖配置，不出现编造结果。
11. 已输出独立 UI 体验优化记录，且 P0/P1 体验问题已在最终报告中提示。
12. 所有关键校验点与 `docs/业务需求` 下的标准答案一致。

---

## 14. 最小回归集

当不需要完整重建 workspace，只需要验证最新业务知识功能是否仍生效时，可执行最小回归集。最小回归集允许复用现有环境，但最终报告必须明确标记为“非全量干净回归”，不能替代完整回归结论：

1. 资产导入校验：
   - 业务词典 `10/10`
   - 外部数据依赖 `4/4`
   - 主 SQL 模板 `11` 个或 `21` 条问题变体
2. 业务词典 alias 用例：
   - “首充 cohort 的二存到六存” -> T08 等价结果
3. 外部依赖用例：
   - “统计 ROI” -> 要求补充投放金额
   - “补充 PV、UV、下载点击UV” -> 要求补充访问PV / 访问UV / 下载点击UV
4. 主模板 smoke：
   - T01
   - T04
   - T09
   - T13
5. 追问 smoke：
   - “那只看非TOP3呢？”
6. 图表 smoke：
   - T01 或 T09 生成图表成功

---

## 15. 备注

- 本计划是面向系统 UI 端到端验证的测试计划，不替代 `test-runbook.md` 中的 SQL 对账说明。
- SQL 对账、造数与表结构准备仍以 `test-runbook.md`、`expected-results.md`、`seed.sql` 为准。
- 若后续 UI 导入链路、workspace 初始化流程、TiDB 建模流程或业务知识配置中心字段发生变化，应同步更新本计划。
- 若用户明确要求跳过 setup，可从 Step 12 开始做资产校验和问数验证，但最终报告必须说明复用了哪个 workspace / knowledge base，并标记为“非全量干净回归”。
