# UI 体验优化记录（2026-05-02）

> 依据：`docs/业务需求/问数回归测试计划.md`。本文件记录 MCP Playwright 端到端测试中发现的真实体验问题。

| 编号 | 批次 | 严重性 | 页面 / 流程 | 问题 | 影响 | 建议 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| B0-UX-01 | B0 | P2 | 建模 AI Assistant / 关联关系导入 | 重复执行关联关系推荐导入后，系统没有提示已有相同关系，导致关系从 68 条变成 136 条，出现 68 个重复关系组。测试数据已人工去重回 68 条、0 重复组。 | 可能污染模型关系、降低用户信任，并可能影响后续问数 join 选择。 | 导入前按 from/to column + join type 做去重；导入弹窗显示新增/重复/覆盖数量；重复项默认跳过或需要用户确认覆盖。 | 待修复 |
| B0-TECH-01 | B0 | P1 | 建模部署 / deploy | 关系去重后重新 deploy 生成 `c70cae742a6603a294f445f46009cc88ae6fb523`，manifest 已是 68 条关系，但部署请求卡住后失败为 `read ECONNRESET`。 | B1 问数如果继续使用旧 `ba87...` 会带着 136 条重复关系，可能导致 Ibis/Trino dry-run 报 Multiple entries with same key；当前已切回 `e88bc...`。 | 部署流程需要超时失败回写/可重试；本轮已临时切回无重复关系的成功 deployHash `e88bc...`。 | 待修复 |
| B1-CASE-01 | B1 | P1 | B1 降级用例清单 | FT02/FT03/FT04/FT07/FT08/FT10 原始 recommended_question 没有完整表达必填槽位或期望粒度，导致补充信息提示或误路由。 | 全量回归执行会被用例质量阻塞，结果不易核实。 | 已修订 `docs/业务需求/csv/16_第一期Excel示例表格降级保存清单.csv`：明确 cohort 日期范围、period_days/n_days、TOPN、user_segment + game_type_id 等粒度。 | 已修订用例 |
| B1-AI-01 | B1 | P1 | 问数策略 / 槽位提取 | FT02-D 使用“D30”仍被判定缺 `period_days`，但提示文案本身又建议“D7/D30”。改成“30天回收周期”后才通过。 | 用户按系统提示补“D30”可能仍无法继续，造成追问循环。 | 已修复：`_extract_template_parameters_from_query` 支持单个 `D7` / `D30` / `首存后D30` 写法；T04 模板文档补充 D30 变体；2026-05-02 定向复测 FT02-D-D30 通过（thread 134 / response 143）。 | 已修复并复测 |
| B1-AI-02 | B1 | P1 | 问数语义 / 模板覆盖 | FT05-D 无法稳定生成“TOP3/非TOP3 分层 + D1/D3...D360 累计收入”的组合表；当前只能生成 TOP3 用户分层或 cohort 累计收入之一。 | 第一期 ROI 回收表中的 TOPN cohort 累计收入降级表无法同形覆盖。 | 新增组合 SQL 模板或 SemanticPlan 组合能力，明确 grain=`user_segment + first_deposit_date + relative_day_no`。 | 待修复 |
| B1-AI-03 | B1 | P1 | 模板路由 / 粒度校验 | FT10-D 原始“按游戏类型”问题一度误路由到所有用户/TOP3/非TOP3 汇总（无 game_type 字段）；明确 `user_segment + game_type_id` 后才通过。 | 表可以保存但业务粒度错误，容易被误判为通过。 | 模板采纳需校验 expected_grain / required_dimensions；游戏类型问题必须要求结果包含 game_type 维度。 | 待修复 |
| B1-UX-01 | B1 | P2 | 问数结果区 / 数据表保存 | 部分已完成回答不会自动让测试脚本看到工作区 tab，需要点击回答卡片中的“查看数据”后才能保存。 | 自动化与用户理解成本增加；如果用户不知道点击“查看数据”，可能以为无法保存。 | 回答完成且有 SQL/预览时，可自动打开结果区或更突出“查看数据 / 保存为数据表”入口。 | 待评估 |
| REG-AI-01 | B5 | P1 | 业务泛化原始用例 | 20 条原始泛化用例因缺租户平台/渠道/时间范围先进入澄清；补槽后 18 条恢复通过。 | 原始用例如果不补槽会被误判为失败；用户需要更顺滑的补充表单/继续执行体验。 | 测试计划中把原始问法和补槽问法分开；UI 补充表单提交后必须自动继续并在同一 thread 产出结果。 | 待评估 |
| REG-AI-02 | B5 | P1 | 语言泛化 / TOPN 口语表达 | LING03-SLOT 补齐租户、渠道、时间、TOP5 后仍出现 `TEXT_TO_SQL_SQL_MISSING`。 | “大户/前5个”等 TOPN 口语表达仍不稳定。 | 已修复：增强 topn_segment 业务词典、R05 分析规则、T09 SQL 模板和运行时 TOPN 口语识别；2026-05-02 定向复测 LING03-SLOT 通过（thread 135 / response 144）。 | 已修复并复测 |
| REG-AI-03 | B5 | P2 | 边界用例 / TOPN 人数不足 | EDGE05-SLOT 仍要求明确“活跃用户定义 / TOPN 排序依据”，未继续生成 SQL。 | 用例口径和系统澄清项不完全对齐。 | 已按策略处理：运行时支持“大户/投注流水最高/前N个”默认映射；测试用例补充“活跃用户按统计期内有有效投注、排序依据为累计有效投注”后通过（thread 136 / response 145）。原始不完整问法仍应进入澄清，不计为失败。 | 显式口径通过 |
| REG-UX-01 | B3 | P1 | 追问 / 数据预览 | B3 对 T09 追问“那只看非TOP3呢？”回答完成且有 SQL，但 UI `preview-data` 多次 500，Trino parser 报 `mismatched input '1'`。 | 用户看到回答但数据预览区可能加载失败，影响保存/导出/核验。 | 已修复：thread response lineage 持久化 `sqlMode`，preview-data 继承源回答 SQL 模式；2026-05-02 定向复测 T09 追问同 thread 通过（thread 140 / response 151，preview 200）。 | 已修复并复测 |
| REG-PERF-01 | B3 | P2 | 数据表 AI 操作 | 数据表 AI 操作在默认 30 秒请求超时下失败；240 秒超时复测 Filter/Cleaning/Grouping/Enrichment 全部成功。 | 功能可用但耗时较长，用户可能误以为失败。 | UI 增加长任务 loading/轮询；后端结构化操作尽量避免进入 AI rewrite 或优化 dry-run 耗时。 | 待优化 |
