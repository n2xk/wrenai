# 问数回归测试结果（2026-05-04）

## 1. 测试目标

本轮按 `docs/业务需求/问数回归测试计划.md` 执行重置数据后的严格 UI 端到端回归，并覆盖 P3 架构升级后的问数链路：

- execution-based 多候选投票
- 复杂查询 query decomposition
- LLM entity extraction schema linking
- 外部数据补充后继续问数
- Excel 降级版数据表保存与 FULL 同形输出
- 图表、看板、反馈、数据表、SQL 查看、Diagnostics 等产品化链路

## 2. 环境与重置证据

- UI：`http://127.0.0.1:3002`，PM2 进程 `test-ui`
- AI Service：`http://127.0.0.1:5555`，PM2 进程 `test-ai-service`
- App PostgreSQL：`127.0.0.1:9432/wrenai`
- TiDB demo：`tidb_business_demo`
- AI Service PM2 环境已确认包含：
  - `WREN_SQL_GENERATION_CANDIDATE_COUNT=2`
  - `WREN_SQL_CORRECTION_CANDIDATE_COUNT=2`
  - `WREN_SQL_EXECUTION_VOTING_ENABLED=true`
  - `WREN_QUERY_DECOMPOSITION_ENABLED=true`
  - `WREN_SCHEMA_LINKING_ENTITY_EXTRACTION_ENABLED=true`
  - `WREN_LEGACY_EXTERNAL_DEPENDENCY_FALLBACK_ENABLED=true`

数据重置动作：

1. 清空 App PostgreSQL 并重新 bootstrap admin / workspace / knowledge base。
2. Drop/Create TiDB `tidb_business_demo`，重新加载 `local_tidb_schema.sql` 与 `seed.sql`。
3. 使用 PM2 重启 UI / AI Service。
4. 执行 TiDB seed 对账：`python3 docs/业务需求/verify_tidb_regression.py --extended-seed`，结果 `33 passed, 0 failed`。

本轮最终 selector：

```json
{
  "workspaceId": "1208b1e9-e442-4257-81be-211b91d7a660",
  "knowledgeBaseId": "089cb1a3-cb14-46b5-8ae4-15f8e8282ebc",
  "kbSnapshotId": "d661c6df-2172-4b80-b438-1b3abfd6778f",
  "deployHash": "09c8b6bc102538ae3a9b83cc6ff0895613a93ce4"
}
```

## 3. B0 导入与生成

已完成：

- 创建 TiDB 知识库与 connector。
- 导入 26 张 TiDB 业务表。
- 导入知识资产：
  - 14 条分析规则
  - 10 条业务词典
  - 4 条外部数据依赖
  - 4 条问数策略
  - 23 条 SQL pairs / 11 个可执行模板
- 生成 suggested questions：6 条。
- Modeling AI assistant 语义说明：selectedModels=26，generatedModels=25，savedModels=25，savedColumns=743。
- Modeling AI assistant 关联关系：recommended=53，mapped=53，skipped=0，saved=53。

原始证据：`wren-ui/tmp/continue-b0-semantics-relationships-result.json`

## 4. 回归批次结果

### B1：第一期 Excel 降级版数据表保存

- 范围：FT01-D ~ FT11-D，共 11 条。
- 口径：严格 UI 端到端，生成 thread / response，并保存到数据表。
- 结果：`11/11 PASS`。
- 证据：`wren-ui/tmp/tidb-b1-spreadsheet-e2e-output/summary.json`

### B2/B4/B5/B6/B3：主用例、泛化、FULL、产品化

原始批次结果：

| 批次 | 总数 | PASS | PARTIAL | FAIL | 说明 |
| --- | ---: | ---: | ---: | ---: | --- |
| B2 | 15 | 14 | 1 | 0 | 核心模板用例基本通过 |
| B4 | 10 | 8 | 0 | 2 | 普通问数里有缺槽/需补充条件路径 |
| B5 | 55 | 36 | 0 | 19 | 泛化用例中多条为缺条件需补充后继续 |
| B6 | 11 | 7 | 2 | 2 | FULL 用例中外部数据/同形输出需补跑 |
| B3 | 11 | 9 | 0 | 2 | 图表固定时机、负反馈 reason 需补跑 |

原始证据：`wren-ui/tmp/tidb-b2-b6-full-e2e-output/summary.json`

### 缺槽补充后回归

- 范围：OQ04-SLOT、PX03-RETRY、PX06/PX07、LING01/03/04/06/10、EDGE01~EDGE12、COMMON-RETRY、FT07/FT08-FULL-RETRY，共 24 条。
- 结果：`24/24 PASS`，全部生成 SQL。
- 证据：`wren-ui/tmp/tidb-b2-b6-full-e2e-output/slot-completed-cases/summary.json`

### B3 产品化补跑

补跑项：

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 图表生成完成后固定到看板 | PASS | chartThreadId=13，chartResponseId=13，dashboardItemId=1 |
| 正/负反馈 | PASS | positiveResponseId=13，negativeResponseId=19，负反馈 reason=`incorrect_data_retrieved` |
| 数据表 AI 操作 | PASS | spreadsheetId=12，FILTER/CLEANING/GROUPING/ENRICHMENT 均 PASS |

证据：`wren-ui/tmp/tidb-b2-b6-full-e2e-output/B3-retry/summary.json`


### OQ09 元数据说明类问题补跑

原始 B4 中 OQ09 曾因脚本等待回答异常记为失败。已用 UI 同源 API 重新补跑并持久化到历史对话：

- question：当前 TiDB workspace 里和充值、提现、投注相关的主要表有哪些？分别大概记录什么？
- threadId=139，responseId=144
- 结果：`FINISHED`，`hasSql=false`
- 判定：这是元数据/说明类 general answer，不需要生成 SQL；因此不计为 SQL 生成失败。

### FULL 外部数据补充同形输出

重启 AI Service 并修正 PM2 环境注入后，严格 UI 补充外部数据表单并继续问数：

| 用例 | 结果 | threadId | responseId | 输出校验 |
| --- | --- | ---: | ---: | --- |
| FT01-FULL-EXTERNAL | PASS | 136 | 139 | 33 列、7 行，包含汇总行，列名列序贴合综合日报 |
| FT02-FULL-EXTERNAL | PASS | 137 | 141 | 22 列、9 行，包含汇总、环比系数、D1~D360 固定周期 |
| FT04-FULL-EXTERNAL | PASS | 138 | 143 | 22 列、9 行，包含 TOP3、环比系数、D1~D360 固定周期 |

证据：`wren-ui/tmp/tidb-full-external-supply-e2e-output-rerun3/summary.json`

## 5. 自动化/单测验证

AI Service：

```bash
cd wren-ai-service
poetry run pytest \
  tests/pytest/core/test_slot_extractor.py \
  tests/pytest/core/test_fixed_order_ask_runtime.py \
  tests/pytest/pipelines/retrieval/test_scope_filters.py \
  tests/pytest/pipelines/generation/test_sql_rules.py \
  tests/pytest/services/test_ask.py -q
```

结果：`144 passed, 1 skipped`。

UI：

```bash
cd wren-ui
yarn test src/features/home/thread/components/clarificationSlotDisplay.test.ts --runInBand
```

结果：`9 passed`。

## 6. 本轮修复/调整摘要

- 修正 PM2 重启脚本：通过 ecosystem 配置重启，确保 `docker/env/test.local` / `docker/pm2.test.config.cjs` 的环境变量变更真正进入进程。
- 明确 `docker/env/test.example` 中 P3 回归 flags，避免测试环境因缺少环境变量导致外部数据策略或 P3 路径不稳定。
- 验证外部数据缺失时会弹补充表单，补充后使用 supplied external deterministic SQL builder 输出 Excel 同形结果。
- 修正/验证 B3 产品化补跑脚本：等待图表完成后再固定、使用合法负反馈 reason、数据表 AI 操作使用真实存在的列。

## 7. 结论

本轮重置数据后的严格 UI E2E 回归已完成：

- B1 降级数据表保存：通过。
- B2/B4/B5/B6 原始失败项中，缺槽类已按补充条件全部通过。
- B3 产品化失败项已补跑通过。
- FULL 外部数据同形输出已补跑通过。
- P3 ask runtime 相关单测通过。

当前未发现阻塞发布的功能失败。仍建议保留的观察项：

1. UI 控制台存在 Ant Design deprecation / form warning，不影响本轮功能结果，但后续可清理。
2. 外部数据 FULL 用例依赖 PM2 环境变量正确注入；已通过脚本修复，后续重启应用应继续使用 `docker/scripts/test-apps-restart.sh`。
3. OpenRouter 仍需遵守测试计划中的节流规则；本轮未将 429 作为业务失败记录。
