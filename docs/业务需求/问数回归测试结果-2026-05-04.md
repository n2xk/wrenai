# 问数回归测试结果（2026-05-04）

## 1. 测试目标

本轮按 `docs/业务需求/问数回归测试计划.md` 在重置数据后执行严格 UI 端到端回归，并覆盖本轮 P3 问数链路改动：

- execution-based SQL 多候选投票完整化；
- query decomposition 完整化；
- LLM entity extraction schema linking；
- 外部数据补充后继续问数；
- Excel 降级版数据表保存与 FULL 同形输出；
- 图表、看板、反馈、数据表、SQL 查看、Diagnostics 等产品化链路。

所有问数用例均按 UI E2E / UI 同源产品 API 口径执行：创建 `asking_task`、落 `thread / thread_response`，并可在 UI 历史或管理页面追溯；AI Service `/v1/asks` 直连不计为通过证据。

## 2. 环境与重置证据

- UI：`http://127.0.0.1:3002`，PM2 进程 `test-ui`。
- AI Service：`http://127.0.0.1:5555`，PM2 进程 `test-ai-service`。
- App PostgreSQL：`127.0.0.1:9432/wrenai`。
- TiDB demo：`tidb_business_demo`。

数据重置动作：

1. `./docker/scripts/test-env-down.sh -v` 清理测试 compose 与 App PostgreSQL 数据卷。
2. 清理 TiDB demo 容器 / 数据卷后重新 `./docker/scripts/test-env-up.sh`。
3. `cd wren-ui && PG_URL=postgres://postgres:postgres@127.0.0.1:9432/wrenai yarn migrate`。
4. `./docker/scripts/test-apps-restart.sh all` 重启 UI / AI Service。
5. 重新创建 TiDB `tidb_business_demo` 并导入 `local_tidb_schema.sql`、`seed.sql`。
6. 执行 seed 对账：`python3 docs/业务需求/verify_tidb_regression.py --extended-seed`，结果 `33 passed, 0 failed`。

本轮最终 selector：

```json
{
  "workspaceId": "93467766-1944-40e8-99dd-90d5ccd60d6a",
  "knowledgeBaseId": "c5813fb4-5cdf-4b06-8075-c8780153c926",
  "kbSnapshotId": "ab3bae30-6b62-4cb4-a6ec-4388057436ed",
  "deployHash": "5e7e7550166799ed4fc1746662a86c115417813a"
}
```

## 3. B0 导入与生成

已完成：

- 创建 TiDB 回归 workspace / knowledge base / connector。
- 导入 26 张 TiDB 业务表。
- 导入知识资产：14 条分析规则、10 条业务词典、4 条外部数据依赖、4 条问数策略、14 条 SQL 模板 / SQL pairs。
- suggested questions：6 条。
- Modeling AI assistant：生成语义说明并保存；生成关联关系并保存。

证据：`wren-ui/tmp/stability-import-generation-b0-result.json`。

## 4. 批次结果

### B1：第一期 Excel 降级版数据表保存

- 范围：`FT01-D ~ FT11-D`，共 11 条。
- 结果：`11/11 PASS`。
- 资产化：全部生成 thread / response，并保存到数据表列表。
- 证据：`wren-ui/tmp/tidb-b1-spreadsheet-e2e-output/final-check.json`。

### B2/B4/B5/B6/B3：主用例、泛化、FULL、产品化

全量 runner 原始结果：

| 批次 | 总数 | PASS | PARTIAL | FAIL | 原始说明 |
| --- | ---: | ---: | ---: | ---: | --- |
| B2 | 15 | 13 | 2 | 0 | T01/T03 为关键字断言未读取 preview data |
| B4 | 10 | 10 | 0 | 0 | 普通问数 / 路由安全通过 |
| B5 | 55 | 54 | 0 | 1 | ROUTE08 被 runner 误按新 thread 首问执行 |
| B6 | 11 | 8 | 3 | 0 | FT01/FT02/FT04 首问正确阻断外部数据缺口 |
| B3 | 11 | 11 | 0 | 0 | 产品化 smoke 通过 |

证据：`wren-ui/tmp/tidb-b2-b6-full-e2e-output/summary.json`。

### B2 PARTIAL 复核

T01/T03 不是业务失败，而是测试断言只看回答正文 / SQL / diagnostics，没有读取 UI 同源 preview data：

| 用例 | 原始状态 | 复核证据 | 结论 |
| --- | --- | --- | --- |
| T01 | PARTIAL：缺 `2026-04-02` | `/api/v1/thread-responses/13/preview-data` 返回 6 行，含 `2026-04-02 00:00:00.000000` 与 `deposit_amount=2080.0000` | PASS |
| T03 | PARTIAL：缺 `990101, 990105` | `/api/v1/thread-responses/14/preview-data` 返回 5 行，含 `player_id=990101` 等首存用户明细 | PASS |

复核输出：`wren-ui/tmp/preview-responses-output.json`。

### B5 ROUTE08 复核

ROUTE08 是追问用例，不能作为独立新 thread 首问执行。已按计划口径在同一 thread 复测：

1. 先问 TOP3 / 非 TOP3 区间汇总基础问题。
2. 同一 thread 追问：`改成TOP5看看`。

结果：`PASS`。

- threadId：106
- baseResponseId：111
- followResponseId：112
- preview：7 行，包含 `TOP5` / `非TOP5` 分层。
- 证据：`wren-ui/tmp/route08-followup-e2e-output/result.json`。

### B6 FULL 外部补数闭环

FT01/FT02/FT04 首问因缺投放 / PV / UV / 下载点击 UV 被阻断，这是安全行为；随后按补充表单继续原问题，专项脚本 3/3 通过：

| 用例 | 结果 | threadId | responseId | preview |
| --- | --- | ---: | ---: | --- |
| FT01-FULL-EXTERNAL | PASS | 103 | 106 | 33 列、7 行，包含汇总行和 PV/UV/投放等外部列 |
| FT02-FULL-EXTERNAL | PASS | 104 | 108 | 22 列、9 行，包含汇总、环比系数、D1~D360 固定周期 |
| FT04-FULL-EXTERNAL | PASS | 105 | 110 | 22 列、9 行，包含 TOP3、环比系数、D1~D360 固定周期 |

证据：`wren-ui/tmp/tidb-full-external-supply-e2e-output-2026-05-04-clean/summary.json`。

FULL 严格校验已合并外部补数证据：`11/11 FULL_PASS`。

证据：`docs/业务需求/第一期Excel_FULL同形严格校验结果-2026-05-04.md`。

### B5 多轮补槽 / 外部补数 / 图表专项复核（追加）

针对通用 runner 无法表达真实多轮路径的问题，已补充执行专项脚本：

```bash
node wren-ui/scripts/tidb-followup-special-cases-e2e.mjs
```

结果：`3/3 PASS`。

| 用例 | 结果 | threadId | 关键证据 |
| --- | --- | ---: | --- |
| ROUTE05 | PASS | 122 | 首问阻断投放金额；同 thread 补 `date,channel_id,ad_spend` 后生成 SQL，`reasonCodes` 含 `supplied_external_roi_sql_selected` |
| ROUTE13 | PASS | 123 | 同 thread 依次补租户、渠道 + 日期 + D7、投放金额 CSV；最终生成 SQL，前值未丢 |
| PX12 | PASS | 124 | 先生成 TOP5 / 非 TOP5 preview，再基于 response 触发图表生成，chart status `FINISHED` |

证据：`wren-ui/tmp/tidb-followup-special-cases-e2e-output/summary.json`。

## 5. 单测 / 静态验证

P3 问数链路改动提交前已验证：

```bash
cd wren-ai-service
poetry run ruff check src/core/fixed_order_ask_runtime.py tests/pytest/core/test_fixed_order_ask_runtime.py
poetry run pytest tests/pytest/core/test_fixed_order_ask_runtime.py -q
python -m compileall -q src/core/fixed_order_ask_runtime.py
```

结果：`100 passed`（最终外部补数修复后），ruff 通过，compileall 通过。

补充表单 / 多轮澄清修复后追加验证：

```bash
cd wren-ai-service
poetry run pytest tests/pytest/core/test_slot_extractor.py tests/pytest/core/test_fixed_order_ask_runtime.py -q
poetry run ruff check src/core/ask_runtime_patterns.py src/core/slot_extractor.py src/core/fixed_order_ask_runtime.py tests/pytest/core/test_slot_extractor.py tests/pytest/core/test_fixed_order_ask_runtime.py
```

结果：`111 passed`，ruff 通过。

本轮新增/修订的 FULL 严格校验脚本已运行通过：

```bash
python3 docs/业务需求/validate_excel_full_coverage.py \
  --ask-summary wren-ui/tmp/tidb-b2-b6-full-e2e-output/ask-summary.json \
  --external-supply-summary wren-ui/tmp/tidb-full-external-supply-e2e-output-2026-05-04-clean/summary.json \
  --output docs/业务需求/第一期Excel_FULL同形严格校验结果-2026-05-04.md
```

## 6. SQL 生成失败结论

本轮完成复核后，没有确认的 SQL 生成失败：

- B2：T01/T03 均为 preview data 断言覆盖问题，SQL 与结果正确。
- B4：10/10 PASS。
- B5：ROUTE08、ROUTE05、ROUTE13、PX12 均按真实同 thread / 图表产品链路复核 PASS。
- B6：FT01/FT02/FT04 首问 BLOCKED_EXTERNAL 属于预期安全阻断，补数后 PASS。

## 7. 结论

本轮重置数据后的严格 UI E2E 回归已完成：

- B0 导入 / 生成完成。
- B1 降级数据表保存：`11/11 PASS`。
- B2 核心模板：复核后 `15/15 PASS`。
- B4 普通问数 / 路由安全：`10/10 PASS`。
- B5 泛化 / 模板竞争：复核后 `55/55 PASS`。
- B6 Excel FULL：补数闭环后 `11/11 FULL_PASS`。
- B3 产品化：`11/11 PASS`。

当前未发现阻塞发布的 SQL 生成失败或产品化链路失败。后续自动化 runner 需要继续收敛两点：

1. 关键字断言必须读取 preview data，避免把正文展示缩写 / 隐藏字段误判为 PARTIAL。
2. ROUTE08、ROUTE13、ROUTE14 等追问类用例必须同 thread 执行，不能按独立首问跑；其中 ROUTE05 / ROUTE13 / PX12 已增加专项脚本覆盖。
