# 问数回归测试结果（2026-04-26 / TiDB workspace）

## 1. 执行范围

本轮按 `docs/业务需求/问数回归测试计划.md` 执行 TiDB 问数回归，且按用户要求：

- **跳过 setup 重建**
- **复用现有 TiDB workspace**
- **通过 MCP Playwright / Chrome 完成问数、追问、图表、阻塞问题验证**

## 2. 当前环境

| 项 | 值 |
| --- | --- |
| 执行日期 | 2026-04-26 |
| workspace_id | `e4fd1d67-59a5-42de-adf2-1777698b5f21` |
| knowledge_base_id | `27ea94ff-415f-4a28-af88-0b0dc226e598` |
| kb_snapshot_id | `27fa6535-b932-4cfc-a231-35bd15d13329` |
| deploy_hash | `5f88d9c5a3d8c23d2280c6f3b9fdf759543f46d0` |
| UI | `http://127.0.0.1:3002` |
| AI Service | `http://127.0.0.1:5555` |
| AI Health | `GET /health -> {"status":"ok"}` |
| UI Health | `GET / -> HTTP/1.1 200 OK` |

## 3. 知识资产沉淀结果

本轮已把现有环境中的分析规则和 SQL 模板沉淀到 `docs/业务需求`：

- `docs/业务需求/当前TiDB workspace知识资产快照-2026-04-26.md`
- `docs/业务需求/knowledge-base/README.md`

当前环境快照结论：

- 分析规则：`R01 ~ R14` **14 条已全部落库**
- SQL 业务模板：**21 条问题变体已落库**
- 主测试模板：`T01/T02/T03/T04/T06/T08/T09/T10/T11/T12/T13`
- 阻塞模板：`T05/T14/T15` 仍保持 `blocked_missing_source`

## 4. 本轮补修问题

在本次完整回归过程中，继续修复了一个剩余问题：

### T15 缺失外部流量指标提示不完整

现象：

- 问题里写了 `PV、UV、下载点击UV、UV下载率、UV注册率`
- 系统只提示了 `访问UV、下载点击UV`
- 漏掉了 `访问PV`

修复：

- 调整 `detect_missing_external_source_requirement()` 中 `PV/UV` 的识别规则，兼容 `补充PV、UV...` 这类紧邻中文字符的问法
- 新增对应单测，覆盖 CJK 邻接场景

涉及文件：

- `wren-ai-service/src/core/fixed_order_ask_runtime.py`
- `wren-ai-service/tests/pytest/core/test_fixed_order_ask_runtime.py`

## 5. MCP Playwright 回归结果

### 5.1 批次汇总

| 批次 | 范围 | 结果 |
| --- | --- | --- |
| `tidb_regression_core1.js` | T02 / T01 / T03 / T08 | **4/4 通过** |
| `tidb_regression_core2.js` | T10 / T04 / T06 / T09 | **4/4 通过** |
| `tidb_regression_core3.js` | T11 / T12 / T13 | **3/3 通过** |
| `tidb_regression_followups.js` | F_T09_NON_TOP3 / F_T04_ONLY_2026_04_02 | **2/2 通过** |
| `tidb_regression_blocked.js` | T05 / T14 / T15 | **3/3 通过** |

最终结果：

- 主模板问数：**11/11 通过**
- 追问：**2/2 通过**
- 缺失外部数据源兜底：**3/3 通过**
- 合计：**16/16 通过**

### 5.2 详细结果

| 编号 | 类型 | 结果 | 说明 |
| --- | --- | --- | --- |
| T02 | 模板问数 | 通过 | 折扣配置 2 行结果正确 |
| T01 | 模板问数 + 图表 | 通过 | 渠道日报数值正确，图表 `MULTI_LINE` 正常 |
| T03 | 模板问数 | 通过 | 首存 cohort 名单正确 |
| T04 | 模板问数 + 图表 | 通过 | cohort 累计收入正确，图表 `MULTI_LINE` 正常 |
| T05 | 阻塞问题 | 通过 | 正确索取 `投放金额`，未编造 ROI |
| T06 | 模板问数 | 通过 | TOP3/非TOP3 玩家分层正确 |
| T08 | 模板问数 | 通过 | 2~6 存人数/率/均额结果正确 |
| T09 | 模板问数 + 图表 | 通过 | ALL/TOP3/非TOP3 区间汇总正确，图表 `GROUPED_BAR` 正常 |
| T10 | 模板问数 + 图表 | 通过 | 首存用户日龄趋势正确，图表 `MULTI_LINE` 正常 |
| T11 | 模板问数 + 图表 | 通过 | 游戏类型分布正确，图表 `BAR` 正常 |
| T12 | 模板问数 + 图表 | 通过 | TOP3/非TOP3 游戏类型分层正确，图表正常 |
| T13 | 模板问数 + 图表 | 通过 | 首存金额分桶正确，图表 `STACKED_BAR` 正常 |
| T14 | 阻塞问题 | 通过 | 正确索取 `投放金额`，未编造首存成本 |
| T15 | 阻塞问题 | 通过 | 正确索取 `访问PV / 访问UV / 下载点击UV` |
| F_T09_NON_TOP3 | 追问 | 通过 | 正确继承上下文，仅返回非TOP3 |
| F_T04_ONLY_2026_04_02 | 追问 | 通过 | 正确继承 cohort 语境，仅返回 `2026-04-02` |

## 6. 运行时观察

### 6.1 已确认修复生效

- `T06` 不再被错误带到 T09 聚合模板
- `T15` 不再漏报 `访问PV`
- `T05/T14/T15` 会直接走缺失外部数据源兜底回答，不编造数据
- 图表生成链路本轮通过

### 6.2 补充稳定性复核

在完整回归通过后，又针对 `core1` 批次做了一次 **MCP Playwright 复跑**，重点复核此前有波动的 `T03 / T08`：

- `T03`：稳定命中 `anchored_template`，`templateId=80`，`fallbackReason=null`
- `T08`：稳定命中 `anchored_template`，`templateId=86`，`fallbackReason=null`

结论：

- 先前 `trusted_reference / template_conflict_low_margin` 的非阻塞观察项已关闭
- 当前 `T03 / T08` 已进入稳定锚定模板执行路径

## 7. 本轮验证命令

### 7.1 Python 定向测试

在 `wren-ai-service/` 执行：

```bash
poetry run pytest tests/pytest/core/test_fixed_order_ask_runtime.py tests/pytest/services/test_deepagents_skill_execution_mode.py -q
```

结果：

- `34 passed`

### 7.2 Playwright 执行文件

执行了以下脚本：

- `.playwright-mcp/generated/tidb_regression_core1.js`
- `.playwright-mcp/generated/tidb_regression_core2.js`
- `.playwright-mcp/generated/tidb_regression_core3.js`
- `.playwright-mcp/generated/tidb_regression_followups.js`
- `.playwright-mcp/generated/tidb_regression_blocked.js`

## 8. 结论

截至 **2026-04-26**，在当前 TiDB workspace 下：

1. 现有环境中的 **分析规则** 与 **SQL 模板** 已沉淀到 `docs/业务需求`
2. 剩余的 `T15` 缺失指标提示问题已补修
3. 按 `docs/业务需求/问数回归测试计划.md` 的主测试范围完成回归
4. 当前回归结论为：**通过**

## 9. 关联文档

- `docs/业务需求/问数回归测试计划.md`
- `docs/业务需求/当前TiDB workspace知识资产快照-2026-04-26.md`
- `docs/业务需求/knowledge-base/README.md`
- `docs/业务需求/官方问题对照与修复建议-2026-04-25.md`
