# 业务泛化人工核验结果

- 执行时间：2026-04-30T17:14:04.443250+00:00
- AI Endpoint：`http://127.0.0.1:5555`
- MDL Hash：`64fa7b5f371bb34f71ade1692062ff43e60456f7`
- 用例数：4
- pass：0
- fail：4

| 用例 | 结论 | query_id | chart_query_id | 核验证据 |
| --- | --- | --- | --- | --- |
| PX01 | fail | c65c9d9e-c1cb-4725-b5e9-c40b11e53480 | - | generated SQL 执行失败：ProgrammingError: (1064, 'You have an error in your SQL syntax; check the manual that corresponds to your TiDB version for the right syntax to use line 1 column 223 near "TIMESTAMP WITH TIME ZONE) >= CAST(\'2026-04-02 00:00:00\' AS TIMESTAMP WITH TIME ZONE) AND CAST(`biz_date` AS TIMESTAMP WITH TIME ZONE) < CAST(\'2026-04-03 00:00:00\' AS TIMESTAMP WITH TIME ZONE)" ') |
| PX05 | fail | ed085d55-9149-49a4-b1ab-e9fdfa3508d6 | - | top_n=None, expected=5；generated SQL 执行失败：ProgrammingError: (1064, 'You have an error in your SQL syntax; check the manual that corresponds to your TiDB version for the right syntax to use line 14 column 62 near "TIMESTAMP)\n AND finished_time < CAST(\'2026-04-27 00:00:00\' AS TIMESTAMP)\n GROUP BY player_id\n),\nbet_agg AS (\n SELECT player_id, SUM(valid_bet_amount) AS total_valid_bet, SUM(win_loss_amount) AS total_win_loss\n FROM dwd_bet_order\n WHERE tenant_plat_id = 990001\n AND channel_id = 990011\n AND settle_status = 1\n AND settle_time >= CAST(\'2026-04-15 00:00:00\' AS TIMESTAMP)\n AND settle_time < CAST(\'2026-04-27 00:00:00\' AS TIMESTAMP)\n GROUP BY player_id\n),\nplayer_stats AS (\n SELECT cp.player_id,\n COALESCE(da.total_deposit, 0) AS total_deposit,\n COALESCE(ba.total_valid_bet, 0) AS total_valid_bet,\n COALESCE(ba.total_win_loss, 0) AS total_win_loss\n FROM channel_players cp\n LEFT JOIN deposit_agg da ON cp.player_id = da.player_id\n LEFT JOIN bet_agg ba ON cp.player_id = ba.player_id\n),\nranked AS (\n SELECT player_id, total_deposit, total_valid_bet, total_win_loss,\n DENSE_RANK() OVER (ORDER BY total_deposit DESC) AS rnk\n FROM player_stats\n),\ngrouped AS (\n SELECT CASE WHEN rnk <= 5 THEN \'TOP5\' ELSE \'非TOP5\' END AS group_type,\n SUM(total_deposit) AS total_deposit,\n SUM(total_valid_bet) AS total_valid_bet,\n SUM(total_win_loss) AS total_win_loss\n FROM ranked\n GROUP BY group_type\n)\nSELECT group_type,\n total_deposit,\n total_valid_bet,\n total_win_loss,\n CASE WHEN total_deposit = 0 THEN NULL ELSE total_valid_bet / total_deposit END AS bet_deposit_ratio,\n CASE WHEN total_valid_bet = 0 THEN NULL ELSE total_win_loss / total_valid_bet END AS kill_rate\nFROM grouped\nORDER BY group_type" ') |
| PX12 | fail | 3fabbb50-b5a7-4a41-a2de-9ccf028bfdbf | - | 缺少 PX05 preview 数据，无法生成图表核验 |
| LING01 | fail | 2c0c0c6d-4dd1-409f-8559-66dcafe441e3 | - | 补租户 query_id=416ea7a3-b3c8-49e5-a243-7058537018cf；generated SQL 执行失败：ProgrammingError: (1064, 'You have an error in your SQL syntax; check the manual that corresponds to your TiDB version for the right syntax to use line 8 column 62 near "TIMESTAMP)\n AND finished_time < CAST(\'2026-04-04 00:00:00\' AS TIMESTAMP)\n)\nSELECT \n d.player_id,\n d.player_username,\n d.times,\n d.amount,\n d.finished_time\nFROM dwd_order_deposit d\nINNER JOIN first_deposit_users f ON d.player_id = f.player_id\nWHERE d.status = 2\n AND d.times BETWEEN 2 AND 6\nORDER BY d.player_id, d.times" ') |
