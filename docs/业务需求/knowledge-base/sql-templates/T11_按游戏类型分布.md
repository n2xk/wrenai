---
kb_asset_type: sql_template
import_target: sql_pair
import_format_version: v1
dialect: tidb_mysql8
parameter_style: colon_named
result_grain: game_type_id
id: T11
title: 按游戏类型分布
report: 游戏类型流水分布
priority: high
status: draft_sql
runtime_sync:
  last_verified_at: 2026-04-26
  sync_source: 当前TiDB workspace知识资产快照-2026-04-26
  workspace_id: e4fd1d67-59a5-42de-adf2-1777698b5f21
  knowledge_base_id: 27ea94ff-415f-4a28-af88-0b0dc226e598
  kb_snapshot_id: 27fa6535-b932-4cfc-a231-35bd15d13329
  deploy_hash: 5f88d9c5a3d8c23d2280c6f3b9fdf759543f46d0
  import_status: imported
  question_count: 1
  record_ids:
    - 92
  asset_kind: sql_template
  source_type: business_import
  template_level: L2
  template_mode: anchored_template
business_signature:
  template_id: T11
  concepts:
    - kill_rate
  features:
    - game_type_distribution
  metrics:
    - valid_bet_amount
    - game_type_user_count
    - kill_rate
  dimensions:
    - game_type_id
  parameter_slots: []
  external_dependencies: []
  positive_cues:
    - 游戏类型分布
    - 游戏类型流水
    - 按游戏类型
  negative_cues:
    - TOP3
    - 首存成本
  expected_grain: game_type_id
source_tables:
  - bds_bet_order_detail
  - game_type
  - game_line
  - game_line_series
parameters:
  - tenant_plat_id
  - channel_id
  - start_date
  - end_date
question_variants:
  - 统计某区间内各游戏类型的有效投注、下注次数、均注金额、输赢、杀率、投注占比
source_documents:
  - 第一期数据报表需求V1.xlsx
  - 数据报表API对应SQL&DSL语句整理.xlsx
  - 数据报表表结构Design_with_comments（4.15 v1.2）.sql
---

# T11 按游戏类型分布

## 模板用途

统计某区间内各游戏类型的有效投注、下注次数、均注金额、输赢、杀率、投注占比。

## 建议问题（可转为 sql_pair.question）

- 统计某区间内各游戏类型的有效投注、下注次数、均注金额、输赢、杀率、投注占比

## 核心表/模型

- bds_bet_order_detail
- game_type
- game_line
- game_line_series

## 参数

- tenant_plat_id
- channel_id
- start_date
- end_date

## SQL 模板

```sql
WITH game_type_agg AS (
    SELECT
        b.tenant_plat_id,
        b.channel_id,
        b.game_type_id,
        COUNT(*) AS bet_times,
        SUM(b.valid_bet_amount) AS valid_bet_amount,
        SUM(b.win_loss_amount) AS win_loss_amount
    FROM bds_bet_order_detail b
    WHERE b.settle_status = 1
      AND b.game_type_id IS NOT NULL
      AND b.tenant_plat_id = :tenant_plat_id
      AND b.channel_id = :channel_id
      AND b.settle_time >= :start_date
      AND b.settle_time < DATE_ADD(:end_date, INTERVAL 1 DAY)
    GROUP BY b.tenant_plat_id, b.channel_id, b.game_type_id
)
SELECT
    a.tenant_plat_id,
    a.channel_id,
    a.game_type_id,
    gt.name AS game_type_name,
    a.bet_times,
    a.valid_bet_amount,
    ROUND(a.valid_bet_amount / NULLIF(a.bet_times, 0), 4) AS avg_bet_amount,
    a.win_loss_amount,
    ROUND(a.win_loss_amount / NULLIF(a.valid_bet_amount, 0), 6) AS kill_rate,
    ROUND(a.valid_bet_amount / NULLIF(SUM(a.valid_bet_amount) OVER (), 0), 6) AS bet_share
FROM game_type_agg a
LEFT JOIN game_type gt
       ON gt.id = a.game_type_id
ORDER BY a.valid_bet_amount DESC, a.game_type_id;
```

## 备注

- 如果性能差，建议补一张玩家-游戏类型-日汇总视图。
- SQL 按 TiDB / MySQL 8 风格编写；导入前需在实际 runtime datasource 下做一次校验。
- 当前可视为 SQL 草案，校验通过后可转为 sql_pair。
