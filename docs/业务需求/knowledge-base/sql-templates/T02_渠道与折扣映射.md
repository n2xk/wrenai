---
kb_asset_type: sql_template
import_target: sql_pair
import_format_version: v2
dialect: tidb_mysql8
parameter_style: colon_named
result_grain: channel_id
id: T02
title: 渠道与折扣映射
report: 综合日报表
priority: medium
status: draft_sql
template_type: anchored_template
required_slots:
  - tenant_plat_id
  - channel_id
  - channel_partner_id
runtime_sync:
  last_verified_at: 2026-04-26
  sync_source: 当前TiDB workspace知识资产快照-2026-04-26
  workspace_id: e4fd1d67-59a5-42de-adf2-1777698b5f21
  knowledge_base_id: 27ea94ff-415f-4a28-af88-0b0dc226e598
  kb_snapshot_id: 27fa6535-b932-4cfc-a231-35bd15d13329
  deploy_hash: 5f88d9c5a3d8c23d2280c6f3b9fdf759543f46d0
  import_status: imported
  question_count: 2
  record_ids:
    - 78
    - 79
  asset_kind: sql_template
  source_type: business_import
  template_level: L2
  template_mode: anchored_template
business_signature:
  template_id: T02
  concepts: []
  features:
    - channel_mapping
    - discount_mapping
  metrics: []
  dimensions:
    - channel_id
    - discount_id
  parameter_slots: []
  external_dependencies: []
  positive_cues:
    - 渠道映射
    - 折扣映射
    - channel discount
  negative_cues: []
  expected_grain: channel_id
source_tables:
  - channel
  - dim_report_channel_data_percent_config
  - tenant_plat
parameters:
  - tenant_plat_id
  - channel_id
  - channel_partner_id
question_variants:
  - 查询渠道名称、渠道商、折扣比例。
  - 查询某平台下各渠道的折扣配置与渠道商信息。
source_documents:
  - 第一期数据报表需求V1.xlsx
  - 数据报表API对应SQL&DSL语句整理.xlsx
  - 数据报表表结构Design_with_comments（4.15 v1.2）.sql
---

# T02 渠道与折扣映射

## 模板用途

查询渠道名称、渠道商、折扣比例，用于补充综合日报表等结果维度。

## 建议问题（可转为 sql_pair.question）

- 查询渠道名称、渠道商、折扣比例。
- 查询某平台下各渠道的折扣配置与渠道商信息。

## 核心表/模型

- channel
- dim_report_channel_data_percent_config
- tenant_plat

## 参数

- tenant_plat_id
- channel_id
- channel_partner_id

## SQL 模板

```sql
SELECT
    c.tenant_id,
    c.tenant_plat_id,
    tp.name AS tenant_plat_name,
    c.id AS channel_id,
    c.name AS channel_name,
    c.channel_partner_id,
    c.channel_partner_username,
    c.rate_agent_id,
    c.rate_agent_username,
    COALESCE(r.percent, 100.0000) AS report_percent,
    ROUND(COALESCE(r.percent, 100.0000) / 100, 6) AS report_percent_ratio,
    CASE WHEN r.id IS NULL THEN 0 ELSE 1 END AS has_percent_config,
    r.modify_time AS percent_config_modify_time,
    c.status AS channel_status,
    c.create_time AS channel_create_time,
    c.modify_time AS channel_modify_time
FROM channel c
INNER JOIN tenant_plat tp
        ON tp.id = c.tenant_plat_id
       AND tp.deleted = 0
LEFT JOIN dim_report_channel_data_percent_config r
       ON r.channel_id = c.id
      AND r.deleted = 0
WHERE c.deleted = 0
  AND c.status = 3
  AND tp.enabled = 1
  AND c.tenant_plat_id = :tenant_plat_id
  AND (:channel_id IS NULL OR c.id = :channel_id)
  AND (:channel_partner_id IS NULL OR c.channel_partner_id = :channel_partner_id)
ORDER BY c.id;
```

## 备注

- `channel_id`、`channel_partner_id` 可传 `NULL` 表示不过滤。
- 若渠道未配置折扣比例，则按 `100%` 输出，便于上层直接做比例折算。
- SQL 按 TiDB / MySQL 8 风格编写；导入前需在实际 runtime datasource 下做一次校验。
- 当前可视为 SQL 草案，校验通过后可转为 sql_pair。
